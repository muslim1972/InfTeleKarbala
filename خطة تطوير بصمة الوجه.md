# خطة تحسين نظام التعرف على الوجه — الحضور والانصراف

## ملخص الوضع الحالي (نتائج البحث والتحليل)

### البنية الحالية
التطبيق يستخدم مكتبة `@vladmandic/face-api` (إصدار `^1.7.15`) وهي نسخة محدثة من `face-api.js` الأصلية، تعمل عبر TensorFlow.js في المتصفح.

**الملفات الرئيسية:**
- [`useFaceDetection.ts`](file:///d:/InfTeleKarbala/src/features/attendance/hooks/useFaceDetection.ts) — هوك التعرف على الوجه (83 سطر)
- [`FaceEnrollment.tsx`](file:///d:/InfTeleKarbala/src/features/attendance/components/FaceEnrollment.tsx) — تسجيل بصمة الوجه (190 سطر)
- [`AttendanceCheckInOut.tsx`](file:///d:/InfTeleKarbala/src/features/attendance/components/AttendanceCheckInOut.tsx) — تثبيت الحدث (798 سطر)
- [`useCamera.ts`](file:///d:/InfTeleKarbala/src/features/attendance/hooks/useCamera.ts) — هوك الكاميرا (83 سطر)

### النماذج المحمّلة حالياً
| النموذج | الحجم | الغرض |
|---------|-------|-------|
| `TinyFaceDetector` | 193 KB | كشف الوجه |
| `FaceLandmark68Net` | 357 KB | نقاط الوجه (68 نقطة) |
| `FaceRecognitionNet` | 6.4 MB | استخراج Descriptor (128D) |

### المعايير الحالية
| المعيار | القيمة | الموقع |
|---------|--------|--------|
| `inputSize` (TinyFaceDetector) | 224 | useFaceDetection.ts:38 |
| `scoreThreshold` (كشف الوجه) | 0.5 | useFaceDetection.ts:38 |
| `distance threshold` (التطابق) | < 0.55 | AttendanceCheckInOut.tsx:274 |
| `EAR threshold` (الرمش) | < 0.30 | AttendanceCheckInOut.tsx:281 |
| `matchFrames` (ثبات بديل) | >= 10 | AttendanceCheckInOut.tsx:281 |

---

## إجابات مفصلة على الأسئلة المطروحة

---

### A. فحص معلومات الوجه المرجعية (Enrollment)

> [!CAUTION]
> **الوضع الحالي ضعيف جداً** — يتم حفظ **descriptor واحد فقط** (128 رقم عشري) من **صورة واحدة فقط** من **زاوية واحدة** وبـ **إضاءة واحدة**.

#### المشاكل الحالية:

1. **وصف واحد (Single Descriptor):** يُحفظ `face_descriptor` كمصفوفة `number[128]` واحدة في جدول `profiles`. هذا يعني:
   - إذا تغيرت إضاءة الغرفة = فشل المطابقة
   - إذا كان الموظف يرتدي نظارات وسجّل بدونها = فشل
   - إذا تغير شكل اللحية أو الحجاب = فشل
   - إذا كانت الكاميرا متسخة أثناء التسجيل = descriptor خاطئ أصلاً

2. **inputSize = 224 صغير:** هذا يقلل من دقة استخراج الملامح. القيمة المثلى هي 320 أو 416 لتحسين الدقة.

3. **لا يوجد فحص جودة (Quality Check):** لا يتم التحقق من:
   - حجم الوجه في الإطار
   - درجة وضوح الصورة (sharpness)
   - درجة الإضاءة
   - عدد الوجوه (قد يكون هناك أكثر من وجه)

4. **لا يوجد SsdMobilenetv1:** الموديل الأدق (`SsdMobilenetv1` — 5.6 MB) **موجود في المجلد** لكنه **غير مُحمّل**! يُستخدم `TinyFaceDetector` الأقل دقة.

#### ما يجب أن يكون عليه (أحدث الممارسات):

- **Multi-Descriptor Enrollment:** حفظ 3-5 descriptors من زوايا مختلفة (أمامي، يمين قليلاً، يسار قليلاً) + إضاءات مختلفة
- **Averaged Descriptor:** حساب المتوسط الحسابي لعدة descriptors لتكون مرجعاً أكثر استقراراً
- **Quality Gate:** رفض الصور الضبابية أو ضعيفة الإضاءة أو التي لا يكون الوجه فيها مركزياً
- **استخدام SsdMobilenetv1 للتسجيل:** لأنه أدق (الدقة أهم من السرعة عند التسجيل لأنها عملية تُنفذ مرة واحدة)

---

### B. عملية تسجيل الحدث — مقاومة الاحتيال

> [!WARNING]
> **آلية مقاومة الاحتيال (Anti-Spoofing) بسيطة جداً وسهلة الخداع.**

#### الوضع الحالي:
- **فحص الحيوية = EAR (Eye Aspect Ratio) < 0.30 أو 10 إطارات ثبات فقط**
- هذا يعني أن أي شخص يضع صورة مطبوعة أمام الكاميرا ويحركها = ينجح في الخداع بعد 10 إطارات
- لا يوجد كشف عمق (Depth Detection)
- لا يوجد كشف ملمس الجلد (Texture Analysis)
- لا يوجد فحص تحدي عشوائي (Challenge-Response)

#### ما يجب تحسينه:

1. **Multi-Challenge Liveness:** طلب حركات عشوائية (ادر رأسك يميناً، ابتسم، ارفع حاجبيك)
2. **Texture Analysis:** فحص بنية سطح الجلد للكشف عن الصور المطبوعة والشاشات
3. **Motion Consistency:** التحقق من أن حركة الوجه طبيعية وليست حركة ورقة
4. **Multiple Blinks:** طلب رمش عدة مرات بدل مرة واحدة
5. **Head Pose Estimation:** التحقق من أن الوجه ثلاثي الأبعاد وليس مسطحاً
6. **Warm-up Period:** إضافة فترة تهيئة للكاميرا والنموذج

---

### C. سبب فشل التعرف في المرة الأولى ونجاحه في الثانية

> [!IMPORTANT]
> **تم تحديد السبب الجذري بشكل واضح — وهو مشكلة تقنية متعددة الأسباب:**

#### السبب الرئيسي: **الكاميرا لم تستقر بعد + النموذج لم يسخن (Cold Start)**

1. **Camera Warm-up Time (السبب الأول والأهم):**
   - في [`AttendanceCheckInOut.tsx`](file:///d:/InfTeleKarbala/src/features/attendance/components/AttendanceCheckInOut.tsx#L341-L349) السطر 341-349:
   ```typescript
   requestAnimationFrame(() => {
     if (videoRef.current && stream) {
       videoRef.current.srcObject = stream;
       videoRef.current.play().then(() => {
         if (isEnrolled) {
           startFaceDetection(action); // ← يبدأ الكشف فوراً!
         }
   ```
   - الكاميرا تُفتح ويبدأ الكشف **فوراً** بدون انتظار استقرار الصورة
   - أول 3-5 إطارات من الكاميرا تكون **سوداء أو ضبابية** (الكاميرا تحتاج وقت لضبط التعرض والتركيز)
   - هذا يعني أن أول عدة محاولات كشف ستفشل حتماً

2. **TensorFlow.js Cold Start (السبب الثاني):**
   - أول عملية inference في TensorFlow.js تكون **أبطأ بكثير** (تسمى "warm-up")
   - النموذج يحتاج أول استدعاء لتهيئة GPU/WebGL pipeline
   - بعد أول استدعاء ناجح، السرعة تتحسن بشكل كبير

3. **matchFrames Counter لا يُعاد ضبطه بين المحاولات:**
   - في السطر 275: `debugStatsRef.current.matchFrames++`
   - العداد يتراكم لكن **لا يُصفَّر** عند خروج الوجه من الإطار ثم عودته
   - هذا يعني أنه حتى لو خرج الوجه وعاد، العداد يتابع من حيث توقف
   - **لكن** في المحاولة الأولى: العداد يبدأ من 0 ويحتاج 10 إطارات ناجحة متوالية
   - في المحاولة الثانية: الكاميرا مستقرة + النموذج ساخن = يصل 10 بسرعة

4. **scoreThreshold = 0.5 مرتفع نسبياً:**
   - مع إضاءة ضعيفة أو كاميرا لم تستقر، يرفض الكثير من الوجوه المشروعة

#### الحل المطلوب:
- إضافة **فترة انتظار (warm-up period)** قبل بدء حلقة الكشف
- تشغيل **warm-up inference** على إطار فارغ لتسخين النموذج
- خفض `scoreThreshold` إلى 0.4 أو 0.35
- إعادة ضبط `matchFrames` إلى 0 عند فقدان الوجه

---

### D. سؤال الرؤية الليلية

> [!NOTE]
> **نعم، ممكن تقنياً لكن بحدود — وبدون كاميرا IR خاصة ستكون النتائج محدودة.**

#### التفاصيل:

1. **بدون كاميرا IR (الوضع الحالي):**
   - يمكن تطبيق **Image Enhancement** على الإطار قبل الكشف:
     - تفتيح الصورة (Brightness boost)
     - تعزيز التباين (Contrast enhancement — CLAHE)
     - تقليل الضوضاء (Denoising)
   - هذا يعمل في **إضاءة خافتة** لكن **ليس في الظلام التام**
   - يمكن تنفيذه في Canvas قبل تمرير الإطار لنموذج الكشف

2. **مع كاميرا IR (الحل الأمثل):**
   - الهواتف التي تدعم Face ID (مثل iPhone) لديها كاميرا IR
   - لكن Web APIs **لا تعطي وصولاً** لكاميرا IR في المتصفح
   - تحتاج تطبيق أصلي (Native App) للاستفادة منها

3. **الحل العملي الموصى به:**
   - تطبيق Image Enhancement قبل الكشف (ممكن بدون hardware إضافي)
   - تشغيل **فلاش الشاشة** (جعل الشاشة بيضاء لامعة أثناء الالتقاط لإضاءة الوجه)
   - هذا **أبسط وأفعل** من أي حل آخر في بيئة المتصفح

---

## خطة التحسين المقترحة

> [!IMPORTANT]
> **جميع التغييرات تعمل على الواجهة الأمامية فقط — لا تعديل على قاعدة البيانات.**
> لكن سأحتاج تأكيدك: **هل حقل `face_descriptor` في جدول `profiles` نوعه `jsonb` أم `float8[]` أم `text`؟**
> أكتب لي الاستعلام التالي:
> ```sql
> SELECT column_name, data_type, udt_name FROM information_schema.columns WHERE table_name = 'profiles' AND column_name = 'face_descriptor';
> ```

---

### المرحلة 1: إصلاح مشكلة الفشل في المرة الأولى (أولوية قصوى)

#### [MODIFY] [`useFaceDetection.ts`](file:///d:/InfTeleKarbala/src/features/attendance/hooks/useFaceDetection.ts)

1. **إضافة warm-up inference:** بعد تحميل النماذج، تشغيل inference وهمي على canvas فارغ لتسخين TensorFlow.js
2. **خفض `scoreThreshold`** من 0.5 إلى 0.35
3. **رفع `inputSize`** من 224 إلى 320 لتحسين الدقة
4. **استخدام SsdMobilenetv1 للتسجيل (Enrollment)** لأنه أدق — والإبقاء على TinyFaceDetector للكشف اللحظي

#### [MODIFY] [`AttendanceCheckInOut.tsx`](file:///d:/InfTeleKarbala/src/features/attendance/components/AttendanceCheckInOut.tsx)

1. **إضافة فترة تهيئة الكاميرا:** انتظار 500ms بعد فتح الكاميرا قبل بدء الكشف
2. **إعادة ضبط `matchFrames` عند فقدان الوجه** لضمان أن العد يبدأ من جديد
3. **إزالة الاعتماد على `requestAnimationFrame`** واستخدام حلقة بفاصل زمني مضبوط (كل 200ms بدل كل frame) لتقليل الضغط على المعالج وإعطاء النموذج وقتاً كافياً

---

### المرحلة 2: تعزيز جودة التسجيل (Enrollment Enhancement)

#### [MODIFY] [`FaceEnrollment.tsx`](file:///d:/InfTeleKarbala/src/features/attendance/components/FaceEnrollment.tsx)

1. **Multi-Angle Capture:** التقاط 3 صور (أمامي + يمين + يسار) بدل صورة واحدة
2. **Quality Gate:** التحقق من:
   - حجم الوجه (يجب أن يكون 30%+ من الإطار)
   - وضوح الصورة
   - إضاءة كافية
3. **Averaged Descriptor:** حساب المتوسط الحسابي لـ 3 descriptors وحفظه
4. **استخدام SsdMobilenetv1** عند التسجيل (دقة أعلى، السرعة ليست مهمة هنا)

---

### المرحلة 3: تعزيز مقاومة الاحتيال (Anti-Spoofing)

#### [NEW] `src/features/attendance/hooks/useLivenessDetection.ts`

1. **Challenge-Response:** طلب حركة عشوائية (رمش، ابتسامة، دوران الرأس)
2. **Texture Analysis:** فحص تنوع ألوان البشرة (الصور المطبوعة لها تنوع لوني أقل)
3. **Motion Consistency:** تتبع حركة landmarks عبر عدة إطارات للتأكد من حركة طبيعية
4. **Multiple Blinks Detection:** طلب رمش 2-3 مرات بدل مرة واحدة

---

### المرحلة 4: تحسين الأداء في الإضاءة الضعيفة

#### [NEW] `src/features/attendance/utils/imageEnhancement.ts`

1. **Brightness Enhancement:** رفع سطوع الصورة تلقائياً إذا كانت مظلمة
2. **Contrast Enhancement:** تطبيق CLAHE (Contrast Limited Adaptive Histogram Equalization)
3. **Screen Flash:** تبييض الشاشة لإضاءة الوجه عند الالتقاط (بديل الرؤية الليلية)
4. **Auto-Detection:** قياس متوسط سطوع الإطار تلقائياً وتطبيق التحسينات عند الحاجة

---

### المرحلة 5: تنظيف الكود وأفضل الممارسات (Vercel Best Practices)

1. **حذف النماذج غير المستخدمة** من `public/models/face-api/` (توفير ~6.4 MB من حجم البناء):
   - `face_expression_model` (329 KB)
   - `age_gender_model` (430 KB)
   - `face_landmark_68_tiny_model` (77 KB)
   - **ملاحظة:** الاحتفاظ بـ `ssd_mobilenetv1_model` لأننا سنستخدمه للتسجيل
2. **Dynamic Import:** المكتبة بالفعل تُحمّل ديناميكياً ✅
3. **Memoization:** تحسين useCallback dependencies
4. **إزالة زر فحص الكاميرا للمطور** من واجهة المستخدم النهائي

---

## ملخص الملفات المتأثرة

| الملف | نوع التغيير | الوصف |
|-------|------------|-------|
| `useFaceDetection.ts` | تعديل | warm-up، معايير محسنة، دعم SSD |
| `AttendanceCheckInOut.tsx` | تعديل | إصلاح cold start، ضبط matchFrames، حلقة كشف محسنة |
| `FaceEnrollment.tsx` | تعديل | تسجيل متعدد الزوايا، فحص جودة |
| `useLivenessDetection.ts` | جديد | نظام فحص حيوية متقدم |
| `imageEnhancement.ts` | جديد | تحسين الصور في الإضاءة الضعيفة |
| `public/models/face-api/` | حذف ملفات | إزالة النماذج غير المستخدمة |

---

## أسئلة مفتوحة تحتاج إجابتك

1. **نوع حقل `face_descriptor` في Supabase:**
   - هل هو `jsonb`؟ `float8[]`؟ `text`؟
   - إذا كان `jsonb` فيمكننا حفظ عدة descriptors في نفس الحقل (مصفوفة من مصفوفات)
   - **الاستعلام المطلوب:**
   ```sql
   SELECT column_name, data_type, udt_name 
   FROM information_schema.columns 
   WHERE table_name = 'profiles' AND column_name = 'face_descriptor';
   ```

2. **هل تريد إضافة حقل جديد مثل `face_descriptors` (جمع) لحفظ عدة descriptors؟**
   - أم نحوّل الحقل الحالي ليحمل مصفوفة من المصفوفات؟

3. **هل يوجد ميزانية لخدمة خارجية مثل Azure Face API أو AWS Rekognition؟**
   - إذا نعم، يمكن إضافة طبقة تحقق إضافية server-side
   - إذا لا، الحل المحلي (client-side) الذي سنبنيه كافٍ لبيئة العمل

---

## خطة التحقق (Verification Plan)

### اختبارات يدوية
- تسجيل وجه جديد والتحقق من حفظ البيانات الصحيحة
- تسجيل حضور من المحاولة الأولى (يجب أن ينجح)
- اختبار في إضاءة ضعيفة
- محاولة احتيال بصورة مطبوعة (يجب أن ترفض)
- محاولة احتيال بصورة على شاشة هاتف آخر (يجب أن ترفض)

### فحوصات تقنية
- التحقق من أن البناء يعمل بدون أخطاء
- التحقق من حجم الحزمة (Bundle Size) بعد التعديلات
- فحص الأمان السيبراني: عدم تسريب بيانات الوجه، التشفير، عدم حفظ الصور غير الضرورية

---

## ملاحظات أمنية مهمة

> [!CAUTION]
> **لن أُجري أي تعديل على قاعدة البيانات بدون تأكيدك المسبق — حسابك مجاني بدون نسخة احتياطية.**

- جميع التغييرات في المرحلة الأولى هي **frontend فقط**
- إذا احتجنا تغيير بنية البيانات (مثل حفظ عدة descriptors)، سأكتب لك الاستعلام وتنفذه بنفسك
- كلمة مرور التسجيل (`FACE_ENROLL_SECRET`) الافتراضية `admin1234` — **يجب تغييرها في ملف `.env`**
