# سجل التطوير — نظام الحضور والانصراف

> توثيق التغييرات وأسبابها الجذرية. أحدث الإدخالات أولاً.

---

## 2026-08-31 (10) — زر «قياس» بلا أثر: القياسات تُخزَّن بمفتاح الإسقاط وتُقرأ بمفتاح الدار

### البلاغ
في طور «الفحص والتشغيل»: شاشة مقياس القدرة البصرية تبقى `-.--` وزر «قياس» بلا أي تأثير ظاهر، مع أن «المحسوب تصميمياً» معروض (‎-11.5 dBm) واللحامات منفذة.

### الجذر — عدم توافق مفاتيح التخزين والقراءة
في [lab.store.ts](src/features/fiber-simulator/store/lab.store.ts): `recordMeasurement` كان يخزّن القياس بمفتاح **`m.dropId`** (معرّف الإسقاط)، بينما كل القرّاء يبحثون بمفتاح **`buildingId`** (معرّف الدار): قراءة الشاشة `lab.measurements[home.buildingId]` (TestingPanel 78)، قائمة الدور (205)، والعدّاد (123). فكانت القراءة **تُكتب فعلاً لكن لا يجدها أحد** — كتاب في رفٍّ خاطئ. (VFL سليم لأنه مفتاحه buildingId في الاتجاهين.)

### الحل
توحيد المفتاح في المخزن: `[m.buildingId]` — تعديل سطر واحد مع تعليق توضيحي. لا تغيير في الواجهات ولا في استهلاك `recordSplice` (تبقى بمفتاح dropId لأنها لحامات إسقاطات فعلاً).

### درس
«الزر لا يفعل شيئاً» قد تعني «الفعل يحدث لكن الأثر لا يُرى»: تتبّع المسارين (كتابة/قراءة) منفصلين حتى التقائهما في المفتاح — عدم توافق المفاتيح بين منتج ومن مستهلك أخطاء صامتة كلاسيكية بلا أي exception.

### النشر والتحقق
بناء نظيف (`FiberSimulatorWorkspace-DhDtnza4.js`، الدخيلة `index-sABEzsor.js`) → SEED → مزامنة → SWAP بتحقق مسبق → تطابق SHA-256 + الحزم الحرجة 200 → حذف `dist_old` والمؤقتات.

### اختبار
Ctrl+F5 → طور الفحص → اختر داراً → «قياس» ⇒ الشاشة تتلون أخضر/أحمر بالقراءة، وعدّاد القياسات يتقدم حتى اكتمال شروط إتمام المشروع.

## 2026-08-31 (9) — «FAT غير متصلة» رغم وقوعها على المسار: مسارات متقاطعة بلا ربط تقاطعات

### البلاغ
بعد إدخال (8): نجح الإصلاح مع FDC ✓، والمستخدم لصق صندوقَي FAT على المسار العمودي فالتقطَا واستقرا — لكن رسالتَي «صندوق FAT غير متصل بالكبينة FDC عبر شبكة الأنابيب» بقيتا في لوحة الفحص الحي.

### الجذر — كل مسار مكوّن معزول في المخطط
`buildNetwork` في [rules.ts](src/features/fiber-simulator/engine/rules.ts) كان يربط رؤوس **كل مسار داخلياً فقط** — لا يوجد أي ربط بين المسارات المختلفة عند تقاطعها أو تلامسها. مسارات المشروع: الرئيسي (L-shape) عند y=74.847 والعموديان يبدآن عند y=75 (فجوة رسم 0.15م) — فبقيت الشبكة **ثلاثة مكوّنات منفصلة**: FDC على الرئيسي والـFATs على العمودي، وDijkstra لا يجد طريقاً بينهما ⇒ «غير متصلة» رغم أن الرسم يُظهر تقاطعاً صريحاً. إصلاح (8) قسّم الحواف داخل المكوّن الواحد لكنه لم يوّلد وصلاً **بين** المكوّنات.

### الحل — ربط التقاطعات/التلامسات بين المسارات
- **(أ) تقاطع فعلي**: مساعد `segmentIntersection` في [geometry.ts](src/features/fiber-simulator/engine/geometry.ts) — لكل زوج قطعتيْن من مسارين مختلفين: إن تقاطعا تُولَّد عقدة عند نقطة التقاطع تُربط بطرفَي القطعتين (تقسيم مزدوج، الأطوال محفوظة). يغطي المسار العرضي الذي **يعبر** الآخر من طرف إلى طرف.
- **(ب) تلامس قريب**: رأس مسار يقع ضمن 0.5م (`JUNCTION_TOL_M`) من قطعة مسار آخر ⇒ وصلة الرأس بنقطة إسقاطه مع تقسيم القطعة. يغطي طرف المسار «المسنود» على جسم آخر بفجوة رسم صغيرة (حالتنا: 0.15م).
الأوزان كلها مسافات هندسية فلا تتأثر دقة المسافات/ميزانية القدرة؛ والفحص البصري (offset) كما هو.

### درس
الرسم المرئي والنموذج الرياضي نظامان منفصلان: تقاطعان يراهما العين قد لا وجود لهما في الـGraph. كل قاعدة جديدة تُبنى على «الشبكة» يجب اختبارها بمشهد متعدد المسارات المتقاطعة لا بمسار واحد.

### النشر والتحقق
بناء نظيف (`FiberSimulatorWorkspace-GA_2RF3E.js`، الدخيلة `index-CHUkRWuR.js`) → SEED → مزامنة → SWAP بتحقق مسبق → تطابق SHA-256 كامل (`7F3FF1D8...C55197F`) + الحزم الحرجة 200 → حذف `dist_old` والمؤقتات.

### متبقٍ على المستخدم (اختبار)
1. تحديث صارم (Ctrl+F5) وفتح المشروع ⇒ رسالتا «FAT غير متصل» تختفيان ويبقى تحذير التغطية 14/16 فقط (سلسلة Drops منفصلة عن هذا الإصلاح).
2. اسحب FAT على أي مسار يعبر غيره ⇒ يتصل فوراً بلا إجراء إضافي.
3. ملاحظة: لو رسمت مستقبلاً مساراً ينتهي بعيداً عن الآخر (>0.5م) بلا تقاطع فسيظهر «غير متصل» — وهذا صحيح هندسياً؛ القرّب أو اعبره.

## 2026-08-31 (8) — «FDC غير متصلة» بعد الالتقاط الصحيح: خلل طوبولوجي في buildNetwork

### البلاغ
بعد إصلاح السحب (إدخال 7): المستخدم أمسك FDC، اقتربت من مسار الحفر **فالتقطت واستقرت في مكانها عند الإفلات** ✓ — لكن لوحة الفحص الحي ما زالت تعرض «الكبينة FDC غير متصلة بالمقسم عبر مسارات الحفر» برغم أنها واقعة على المسار فعلاً (وأيضاً رسالتا «FAT غير متصل»).

### الجذر — نقاط الإسقاط معزولة عن الشبكة
`attachAnchor` في [rules.ts](src/features/fiber-simulator/engine/rules.ts) يربط المرساة (الكبينة/FAT) بنقطة إسقاطها على أقرب ضلع، **لكنه لم يكن يوصل نقطة الإسقاط بطرفَي الضلع**. فإذا وقع الإسقاط **وسط الضلع** (لا على رأسه) صارت نقطة الإسقاط عقدة معزولة تماماً عن شبكة رؤوس المسارات ⇒ `distance()` عبر Dijkstra ترجع null ⇒ «غير متصلة» **ظلماً** رغم أن الكابينة تبعد 2م عن المسار ومتصلة منطقياً.
الكود القديم «نجح» مصادفةً لأن الالتقاط القديم كان دائماً على رؤوس المسارات (نقاط نهاية الأضلاع) فكان الإسقاط على رأس قائم؛ **ميزة الالتصاق الحر الجديد بالمسار (إدخال 7) كشفت الخلل الكامن** — النجاح المصادفة كان يخفيه.

### الحل — تقسيم الحافة (edge splitting)
تتبّع طرفَي أقرب ضلع برقمَيهما في مصفوفة العقد (`a`/`b`) أثناء البحث عن الإسقاط، ثم بعد إضافة عقدة الإسقاط: `link(proj, a)` + `link(proj, b)` بأوزان المسافتين الجزئيتين ⇒ الإسقاط يصبح **مفصلاً وسط الضلع** موصولاً بالشبكة من الجهتين، والمسافة المقسمة تكافئ طول الضلع الأصلي فلا تتأثر دقة حساب الأطوال.

### درس
«نجاح» ميزة قائمة قد يكون مصادفةً مقتصرة على حالة خاصة (الالتقاط على الرؤوس فقط)؛ إضافة حالة عامة جديدة (الالتصاق الحر بأي نقطة على الضلع) تختبر المسارات التي لم تُمَس قبلاً — والاختبار يجب أن يشمل التوليفات بين الميزات لا الميزة وحدها.

### النشر والتحقق
بناء نظيف (`FiberSimulatorWorkspace-Dfn4yYyb.js`، الدخيلة `index-3dZQTpoY.js`) → SEED → مزامنة + `put index.html` → SWAP بتحقق مسبق → تطابق SHA-256 كامل (`2D3576FB...4F9B610`) + الحزم الحرجة 200 → حذف `dist_old` والمؤقتات.

### متبقٍ على المستخدم (اختبار)
1. حدّث المتصفح (Ctrl+F5) ثم افتح المشروع ⇒ رسالة «FDC غير متصلة» و«FAT غير متصل» تختفي للعناصر الواقعة على المسار.
2. **ملاحظة**: FAT الأولى (قرب x:96) ستبقى بحذار مشروع «تبعد 5.6م عن المسار» — لأنها فعلاً خارج حد 5م؛ اسحبها بالسحب الجديد نحو المسار فيلتصق ويختفي التحذير.
3. تحقق أن أسعار BOQ والتغطية لم تتغير (الإصلاح حساب مسافات فقط ولا يعدّل الكيانات).

## 2026-08-31 (7) — إصلاح جذري للسحب: قفزة العناصر إلى الركن + غياب الالتصاق والتغذية

### البلاغ
بأداة التحديد: إمساك FDC وتحريكها قليلاً ثم إفلاتها ⇒ **تقفز إلى ركن مساحة العمل** (شريط الحالة أظهر x:17/y:52)، بلا أي تحسس للمسار أو تغذية في شريط الحالة أثناء السحب، والسلوك نفسه مع كل الأجزاء. والمتوقع: تحريك الكابينة قليلاً فتلتقط نقطة الارتباط ويخبر شريط الحالة.

### الجذر الثلاثي — خلل فضاء إحداثيات واحد
مجموعات المنشآت/الكبائن/FAT كانت مرسومة **عند أصل الـ Layer (0,0)** ومحتواؤها بإحداثيات مطلقة داخلها:
1. **القفزة**: عند الإفلات تُقرأ `node.x()/y()` المحلية — وهي إزاحة حركة المؤشر من الأصل (أمتار قليلة) — وتُخزَّن في المخزن كإحداثية عالمية ⇒ الكيان ينقلب إلى الركن قرب (17, 52). تطابق بلاغ المستخدم حرفياً.
2. **عدم التحسس**: نفس القيم المحلية الصغيرة تُقارن بمسارات بعيدة عن الأصل ⇒ `nearestTrenchPoint` لا تجد هدفاً أبداً.
3. **صمت شريط الحالة**: `onDragMove` لا يحدّث `hover` إطلاقاً.

### الحل ([SimCanvas.tsx](src/features/fiber-simulator/ui/SimCanvas.tsx))
- **إعادة تموضع المجموعات الثلاث**: `<Group x={s.x} y={s.y}>` والمحتوى بإزاحات نسبية (`Rect x={-w/2}`...) ⇒ `node.x()/y()` أثناء السحب = الإحداثي العالمي مباشرة، والإمساك يتم من جسم العنصر لا من أصل الخريطة. (وreact-konva لا يعيد تطبيق prop لم يتغير فلا مقاطعة للسحب عند تحديث الحالة أثناءه.)
- **الالتقاط الحي عبر `computeSnap` الكامل** (عقد ← مسارات حفر ← شوارع ← شبكة) مع **استثناء العنصر المسحوب نفسه** من العقد (`nodeKeyOf`) كي لا يعلق في موضعه الأول، والالتصاق فقط عند `kind !== 'free'` (الحركة الحرة تبقى حرة).
- **تغذية لحظية**: `setHover` + حالة `dragSnap` جديدة ⇒ شريط الحالة يعرض x/y و«الالتقاط: مسار حفر/عقدة/...» أثناء السحب، ومؤشر الالتقاط الدائري يظهر أثناءها (`snapCursor = dragSnap ?? hover.snap` المشروط بأدوات الرسم).

### درس معماري
في Konva داخل Layer مُحوَّل (translate+scale): **إحداثيات `node.x()` فضاء الأب المحلي** — لا تُخزَّن كعالمية أبداً؛ الحل الصحيح جعل فضاء الأب للعقدة المسحوبة هو فضاء العالم نفسه (المجموعة على إحداثيات الكيان، المحتوى نسبي).

### النشر والتحقق
بناء نظيف (`FiberSimulatorWorkspace-CXhdFGn9.js`، الدخيلة `index-4ET-nmlW.js`) → SEED → مزامنة + `put index.html` → SWAP بتحقق مسبق → تطابق SHA-256 (`A8FA76FD776AC113`) + الحزم الحرجة الست 200 → حذف `dist_old` والمؤقتات.

### متبقٍ على المستخدم (اختبار)
1. أداة التحديد → اسحب FDC نحو المسار ⇒ تلتصق ويظهر «الالتقاط: مسار حفر» وتتحدث x/y في شريط الحالة.
2. الإفلات على المسار ⇒ تبقى مكانه (لا قفزة للركن) وتختفي رسالة «غير متصلة».
3. النحو مع المناهل/الهاند هول/FAT.
4. Ctrl+Z يتراجع عن التحريك.

## 2026-08-31 (6) — بلاغ المحاكي: التقاط مسارات الحفر + تحريك العناصر بالسحب + محاور x/y

### 1) محاور شريط الحالة «س/ص» → «x/y»
قراءة الإحداثيات أسفل اللوحة كانت ترمز بالمحاور العربية «س/ص» — استُبدلت بالرمزين القياسيين «x/y» لطلب المستخدم (تأشير 1 في الصورة: شريط الإحداثيات).

### 2) الجذر: `computeSnap` لا يعرف مسارات الحفر إطلاقاً
بلاغ «FAT لا يقبل فوق المسار إلا بجانبه» — **السبب الجذري المؤكد**: محرك الالتقاط ([geometry.ts](src/features/fiber-simulator/engine/geometry.ts)) كان يلتقط: عقداً قائمة ← محاور شوارع ← شبكة، **دون مسارات الحفر المرسمة** — لذلك المؤشر لا يلتصق بالمسار أبداً بينما قاعدة الفحص الحي تشترط بقاء FAT ضمن 5م منه (`MAX_OFFSET_FROM_DUCT_M`)، فيقع دائماً «بجانبه» ويُرفض.
**الحل**: خيار `paths?: Vec2[][]` في `SnapOptions` بـ kind جديد `path` (تسمية «مسار حفر» في شريط الحالة)، بأولوية بعد العقد وقبل الشوارع. `SimCanvas` يمرر `entities.trenches.map(t => t.points)` فيستفيد وضع FAT/FDC والمنشآت **ورسم المسارات نفسها** (التصاق نهايات المسارات ببعضها = تقاطعات صحيحة). مُفعَّل مؤشر الالتقاط لأداتَي FAT/FDC كي يرى المستخدم نقطة الالتقاط قبل النقر.

### 3) تحريك العناصر بالسحب (طلب 3+4)
- **المخزن**: فعل `moveEntity(kind, id, x, y)` للمنشآت/الكبائن/FAT — تغيير كيانات صرف فيشموله التراجع/الإعادة تلقائياً.
- **اللوحة**: مجموعات المنشآت والكبائن وFAT صارت `draggable` عند أداة التحديد، مع **التقاط حي أثناء السحب** (`onDragMove`): إسقاط الموضع على أقرب مسار حفر ضمن سماحية `max(2م, 12px÷المقياس)` فيلتصق الصندوق بالنقطة الصحيحة لحظة اقترابه، ثم يُعتّم الموضع في `onDragEnd`. التقطّ عند الوضع الجديد يجيب الطلب 4 نفسه (الوضع والتحريك كلاهما يلتقطان).

### حدّ معرفي
تحريك FAT لا يسحب معه نقاط إسقاطات الدار القائمة (تُرسم بالإحداثيات المطلقة) — إن ظهر انفصال بصري يُعاد رسم الإسقاط؛ يُذكر هنا لأنه سلوك مقصود غير مغطى بهذا التنفيذ.

### النشر والتحقق
بناء نظيف (الدخيلة `index-u05_87cJ.js`، `FiberSimulatorWorkspace-BhnR0OCA.js`) → SEED → مزامنة + `put index.html` → SWAP بتحقق مسبق → تطابق SHA-256 كامل (`32DE682BD037489A`) واسم الحزمة واحداً + الحزم الحرجة 200 → حذف `dist_old` والمؤقتات.

### متبقٍ على المستخدم (اختبار)
1. شريط الحالة يعرض x / y.
2. وضع FAT/FDC فوق مسار حفر ⇒ يلتقطه المؤشر (الالتقاط: مسار حفر) ويسلم الفحص الحي.
3. سحب FAT/كبينة/منشأة بأداة التحديد ⇒ تحريك مع التصاق حي بالمسار، وتراجع Ctrl+Z يعمل.
4. رسم مسار جديد قرب مسار قائم ⇒ نهاياته تلتصق به.

---

## 2026-08-31 (5) — بلاغ بعد (4): التلميحات اختفت كلياً والشريط باقٍ — درس Fullscreen API

### العطل: لماذا اختفت التلميحات رغم بوابة document.body؟
**السبب الجذري (مؤكد بالكود)**: المحاكي يستدعي `requestFullscreen()` على **حاويته الخاصة** (`FiberSimulatorWorkspace` سطر ~171) عند التشغيل. في وضع Fullscreen API المتصفح لا يصيّر **إلا ذرية العنصر المكبَّر** — وبوابة `createPortal(..., document.body)` تضع التلميحة **خارج** الحاوية المكبَّرة فتُنشأ في الـ DOM لكنها غير مرئية إطلاقاً. (لهذا لم يظهر العطل في EmployeeSearch — لا يُستخدم داخل ملء شاشة).
**الحل**: هدف البوابة صار `document.getElementById('fiber-sim-root') ?? document.body` — أُضيف `id="fiber-sim-root"` لجذر المحاكي. التلميحة `position: fixed` داخل الجذر: لا يقصّها `overflow-y-auto` للشريط (سياقها الـ viewport لا سلسلة الأسلاف)، وتظهر في ملء الشاشة لأنها داخل العنصر المكبَّر.
**الدرس**: «البوابة إلى body» ليست حلاً كلياً — داخل Fullscreen API الهدف إلزامياً ذرية العنصر المكبَّر.

### الشريط العمودي الباقي: من الأزرار نفسها لا التلميحات
بعد انتقال التلميحات للبوابة ظل الشريط — لأن 17 زراً (40px + فجوات) تفيض على الشاشات القصيرة بمعزل عن التلميحات. **الحل**: تكثيف الشريط (أزرار `h-10`→`h-9`، فجوات `gap-1`→`gap-0.5`، فواصل `my-1`→`my-0.5`، حشوة `py-2`→`py-1.5`) ليتماسك على ارتفاع ≥712px، مع إخفاء شريط التمرير بصرياً `[scrollbar-width:none] [&::-webkit-scrollbar]:hidden` — العجلة/اللمس يظلان يمرران إن فاض المحتوى (كلوحة طرق الحفر المفتوحة على شاشات قصيرة جداً).

### النشر والتحقق
بناء نظيف (الدخيلة `index-BmRPhPJi.js`، `FiberSimulatorWorkspace-CfEMlQk5.js`) → SEED → مزامنة + `put index.html` → SWAP بتحقق مسبق → تحقق حي: تطابق SHA-256 كامل (`A02F6EA664D1FA71`) واسم الحزمة واحداً والحزم 200 → حذف `dist_old` والمؤقتات.

### متبقٍ على المستخدم (اختبار)
1. داخل المحاكي (بملء الشاشة): مرور الماوس على أزرار الشريط الأيمن ⇒ التلميحات تظهر كاملة فوق/تحت الزر.
2. الشريط الجانبي بلا شريط تمرير عمودي مرئي.

### ملاحظة عملية
تعديلان متوازيان على نفس الملف عبر SearchReplace تداخلا وفُقد أولهما — التعديلات على ملف واحد تكون تسلسلية حصراً.

---

## 2026-08-31 (4) — تلميحات المحاكي تُقصّها شريط التمرير + خيار «مشروع جديد» في نافذة الفتح

### 1) عودة التلميحات للتقص: السبب `overflow-y-auto` في شريط الأدوات نفسه
بعد إدخال التلميحة المخصصة (إدخال 3) ظهر شريط تمرير عمودي داخل شريط الأدوات يحدّ التلميحة ويقصّها. **السبب الجذري**: حاوية `SimToolbar` تحمل `overflow-y-auto` (لأزرار كثيرة على شاشات قصيرة) — أي عنصر `absolute` داخلها يُقصّ بحدودها وكل تلميحة تتجاوز ارتفاع الشريط تُولّد شريط تمرير. نفس عائلة «السلف الحابس» لكن هذه المرة **قصّ احتواء** لا حبس سياق `fixed`.
**الحل**: التلميحة صارت تُصيَّر عبر `createPortal(..., document.body)` بـ `position: fixed` محسوبة لحظة `onMouseEnter` من `getBoundingClientRect` للزر: تثبيت بـ `right` (مع حد أدنى 8px من حافة الشاشة)، فوق الزر (`bottom`) ما لم يكن قريباً من الحافة العليا (top < 110) فتنقلب تحته (`top`) — خارج شريط التمرير كلياً فلا قصّ ولا شريط. تختفي في `onMouseLeave`.

### 2) نافذة «فتح مشروع» بلا مشروع جديد — أُضيف الخيار
مربع «مشاريعك المحفوظة» كان يعرض المحفوظ فقط، ولا سبيل لبدء صفحة بيضاء إن وُجد مشروع مفتوح. أُضيف أعلى القائمة زر مميز بإطار متقطع **«مشروع جديد — مساحة عمل فارغة»**: يفرّغ التصميم (`st.clearAll()`)، يعيد الطور إلى المدني، يصفّر الاسم والمعرّف (`projectId=null`) فينشئ الحفظ القادم مشروعاً مستقلاً (منطق Save-As القائم)، مع `suppressDirty` كي لا يوسم الفراغ «غير محفوظ»، وتأكيد إن وُجدت تغييرات غير محفوظة قبل التجاهل.

### النشر والتحقق
بناء نظيف (الدخيلة `index-C_1o44Mo.js`، `FiberSimulatorWorkspace-BE91Dnbj.js`، `EmployeeSearch-Pn3Ub07z.js`) → SEED → مزامنة + `put index.html` → SWAP بتحقق مسبق بالحزم الحرجة → تحقق حي: تطابق SHA-256 كامل (`E0CE9E8F0DC27154`) وطول متساوٍ (1623 بايت) واسم الحزمة الدخيلة واحداً في المحلي والمخدوم + الحزم الحرجة 200 → حذف `dist_old`.
**ملاحظة تحقق**: مطابقة `RawContentStream.ToArray()` مباشرة أعطت تطابقاً زائفاً بعدم التطابق هذه المرة (البايتات تبدأ `3C21646F63747970` = `<!doctyp` لكن الهاش اختلف — سلوك غير موثوق مع الاستجابات المضغوطة)؛ الموثوق هو فك `r.Content` بـ UTF-8 ثم الهاش والمقارنة — تمّ كذلك وتطابق.

### متبقٍ على المستخدم (اختبار)
1. تلميحات أزرار المحاكي تظهر كاملة بلا شريط تمرير عمودي داخل الشريط الجانبي.
2. «فتح مشروع» ⇒ زر «مشروع جديد» أعلى القائمة يفرّغ المساحة ويعيد الطور المدني، ويطلب تأكيداً إن كانت هناك تغييرات غير محفوظة.

---

## 2026-08-31 (3) — ثلاث تحسينات على الحقل العالمي وتلميحات المحاكي

### 1) اتجاه فتح ذكي لقائمة اقتراحات الحقل العالمي
في [EmployeeSearch.tsx] (ينطبق على كل استخدامات الحقل تلقائياً): لحظة الفتح تقاس مساحة الـ viewport أسفل الحقل وأعلاه من `getBoundingClientRect`؛ إن ضاقت المسافة تحت (أقل من ~220px) وكانت الأعلى أكبر انفتحت القائمة **لأعلى** (تثبيت بـ `bottom` بدل `top`) مع قصّ `maxHeight` للمساحة المتاحة وقلب حركة الظهور (`slide-in-from-bottom-2`). فلم تعد القائمة تُقتطع أسفل نوافذ مثل «صلاحيات الحقول».

### 2) سبب ضياع ميزة «يبدأ بـ أولاً» من البحث — وحلّها الجذري في الـ RPC
**السبب (مؤكد بالكود)**: الترتيب «الأسماء التي تبدأ بالحروف أولاً» كان موجوداً في `useEmployeeSearch.ts` لكنه **بعد الجلب**؛ والمسار الافتراضي (RPC `search_available_profiles`) يرتب أبجدياً في SQL ويقتطع `LIMIT` قبل أي ترتيب محلي — فلا تصل الأسماء المطابقة أصلاً إلى الترتيب المحلي. كل استخدام جديد يسلك المسار الافتراضي ف«يفقد» الميزة.
**الحل الجذري**: نقل ترتيب الملاءمة **إلى داخل الـ RPC** (`sql-files/search_available_profiles_prefix_order.sql` طُبّق على قاعدة الـ VPS): رتب 0 = الاسم يبدأ بمصطلح البحث، 1 = الرقم الوظيفي يبدأ به، 2 = اسم المستخدم يبدأ به، 3 = احتواء عام — ثم الأبجدية داخل كل مستوى. **تحقق حي**: `بحث 'عب'` أعاد «عباس شافي...» و«عباس عنون...» أولاً ثم المتضمنات.
**درس تقني مهم**: النص العربي في ملفات SQL يُتلف عبر أنبوبة PowerShell→plink (تحول إلى `؟؟`) — أول تحقق أعاد 0 صفوف زائفاً؛ التحقق الموثوق يتم بإرسال الملف base64 وفكّه على الخادم (`echo <b64> | base64 -d | docker exec -i ...`).

### 3) تلميحات أزرار المحاكي (يمين الشاشة) لا يغطيها المؤشر بعد اليوم
كانت تستخدم `title` الأصلية التي يرسمها النظام قرب المؤشر فيغطيها. في [SimToolbar.tsx]: استُبدلت بتلميحة مخصصة `group-hover` تظهر **فوق الزر** (مثبتة يميناً كي لا تفيض عن حافة الشاشة)، وتنقلب **تحت الزر** ذكياً إذا كان الزر قرب الحافة العليا (top < 110px)، مع `pointer-events-none` وظل وحلقة تنسجم مع ثيم المحاكي الداكن.

### النشر
بناء نظيف (chunks: `EmployeeSearch-Ia2dMvG8`، `FiberSimulatorWorkspace-BojUBQHj`، `TabManageEmployees-CCMirsuy`) → SEED → مزامنة + **`put index.html` صريح** (أصبح جزءاً ثابتاً من سكربت النشر درءاً لفخ criteria=size) → SWAP → تحقق: تطابق SHA-256 (`420538450D8FE2D4`) بين index.html المحلي والمخدوم + chunk البحث يُخدَم 200 → حذف `dist_old` والمؤقتات.

### متبقٍ على المستخدم (اختبار)
1. البحث داخل «صلاحيات الحقول» ⇒ القائمة تفتح لأعلى فوق الحقل (المساحة تحتها ضيقة)، وفي شريط البحث العلوي تفتح لأسفل كالصورة 2.
2. نتائج البحث تبدأ بالمطابقة الاسمية للحروف المكتوبة أولاً.
3. تلميحات أزرار المحاكي تظهر فوق الزر دون أن يغطيها المؤشر، وتنقلب تحت الأزرار العلوية.

---

## 2026-08-31 (2) — إصلاحان بعد النشر: اختفاء أسفل نافذة صلاحيات الحقول + انهيار النظام عند حقل البحث

### العطل 1: أسفل النافذة يختفي تحت شريط الأخبار والفوتر
**الدليل من الصورة**: عناصر flow عادية (TipsMarquee والفوتر الأخضر) ترسم فوق نافذة `fixed inset-0 z-[99999]` — مستحيل نظرياً إلا إذا كانت النافذة **محبوسة في سياق تكديس/احتواء لسلف** (transform/backdrop-filter يجعلان `fixed` مرتبطاً بالسلف لا بالـ viewport؛ حاوية لوحة الإدارة تحمل `animate-in slide-in-from-bottom-4`).
**الحل الحاسم**: `FieldPermissionsModal.tsx` صار يُصيَّر عبر `createPortal(..., document.body)` — النافذة خارج أي سلف حابس مطلقاً، بغض النظر عن هوية السلف.

### العطل 2: «ينهار النظام» عند استخدام حقل البحث
**السبب الجذري (تأكيد بالكود لا تخمين)**: في `EmployeeSearch.tsx` أُضيف `portalClassName` للـ interface في الجولة السابقة لكنه **لم يُفكَّك في معاملات الدالة**، بينما يُستخدم كمعرِّف عارٍ داخل `cn(...)` في قائمة الاقتراحات → أول ظهور للقائمة (أول كتابة تُعيد نتائج) يرمي `ReferenceError: portalClassName is not defined` → لا يوجد ErrorBoundary في المشروع → unmount للتطبيق كاملاً (شاشة بيضاء). vite build لا يفحص الأنواع (esbuild) فمرّ الخطأ صامتاً إلى الإنتاج.
**الحل**: إضافة `portalClassName` إلى التفكيك. (خطأ من نوع «prop في interface بلا تفكيك» — سلوك esbuild/TSC: المبني ينجح، والعطل وقت التشغيل فقط).
**استبعاد مثبت بالفحص على قاعدة الـ VPS**: RPC `search_available_profiles` سليم (SECURITY DEFINER، 2.6ms، `available_profiles` = 355 صفاً، EXECUTE ممنوح لـ authenticated/anon) — العطل لم يكن في القاعدة إطلاقاً.

### النشر والتحقق + درس نشر
بناء نظيف → SEED → `synchronize -criteria=size` → SWAP. **فخ جديد**: `index.html` تغيّر محتواه (هاش الحزمة) دون تغيّر **الحجم** فتجاهلته `criteria=size` وظلّ الموقع يخدم القديم — الحل: `put index.html` صريح (لاحظ: نسخة WinSCP على الجهاز لا تدعم `-overwrite`؛ الرفع يكتب فوق الموجود افتراضياً). التحقق: **تطابق SHA-256** بين `dist/index.html` المحلي والمخدوم حياً + `EmployeeSearch-D6GypwqZ.js` يُخدَم 200 → حذف `dist_old` والمؤقتات.

### متبقٍ على المستخدم (اختبار)
1. فتح «صلاحيات الحقول» ⇒ النافذة كاملة فوق الفوتر (لا اختفاء أسفلها).
2. الكتابة في حقل «منح فردي» ⇒ تظهر الاقتراحات دون أي انهيار؛ اختيار موظف يضيفه كشريحة، و«حفظ» يثبّت المنح الفردي.

---

## 2026-08-31 — تفعيل المستويات لغير المطورين + المنح الفردية (بحث موظف) لمحاكي FTTH

### السياق
عبارة «لم أمسّه عمداً» في إدخال الأمس كانت تشير إلى: سياسات RLS الصارمة تجعل مستويات `isFieldReadOnly` بلا أثر لغير المطورين (قراءة `field_permissions` محصورة بالمطور)، ولم تُفتح حينها تفادياً لتغيير سلوك دون إذن. بطلب صريح من المستخدم نُفِّذ الآن التفعيل الكامل مع طلب جديد ثالث: **منح فردي لمستخدم محدد** بإعادة استخدام حقل البحث العالمي `EmployeeSearch` (بدون برمجة بحث جديد).

### القاعدة (`sql-files/field_user_permissions_and_levels.sql` — طُبِّق فعلياً عبر psql، تحقق ديناميكي أخضر)
1. **فتح القراءة فقط** لـ `field_permissions` على المصادقين: DROP «Strict field_permissions read» → CREATE «Authenticated field_permissions read» `FOR SELECT TO authenticated USING (true)`. سياسة التعديل الصارمة بقيت كما هي (المطور وحده يعدّل) — وهكذا تعمل مستويات `isFieldReadOnly` للمالية/الموارد/الإعلام/العام فعلياً لأول مرة.
2. **جدول `field_user_permissions`**: `(column_name, user_id uuid→profiles.id CASCADE, granted_by→profiles.id SET NULL, created_at, PK(column_name,user_id))` — RLS مفعّلة بسياسة وحيدة صارمة `Strict field_user_permissions all` (`get_my_admin_role() = 'developer'`) + `REVOKE FROM anon`. تحقق مسبق: `profiles.id` = **uuid**.
3. **RPC موحد `get_ftth_simulator_access()`**: `SECURITY DEFINER/STABLE/SET search_path=public`، يعيد `{permission_levels, user_granted}` بنداء واحد للمصادقين فقط (`REVOKE PUBLIC/anon`, `GRANT authenticated`)؛ نداء anon يعيد NULL (مُتحقق). أُسقطت الدالة القديمة `get_ftth_simulator_permission_levels`.
4. ترتيب حرج احتُرم: SQL أولاً ثم نشر الكود (الخطاف الجديد يستدعي الدالة الجديدة).

### الكود
- **`useFtthSimulatorAccess.ts`**: النداء صار `rpc('get_ftth_simulator_access')`؛ المنطق: `allowed = !error && (user_granted === true || evaluateFtthAccess(user, {permission_levels}))` — المنح الفردي **إضافي** لا يلغي تقييم المستويات. كاش 60 ث كما هو.
- **`EmployeeSearch.tsx`**: prop اختياري وحيد `portalClassName` (ملحق عبر `cn/twMerge` بعد الصنف الافتراضي) — يرفع z-index للقائمة المنسدلة (`z-[9999]` الافتراضية) فوق النافذة `z-[99999]` دون أي تغيير على الاستخدامات القائمة.
- **`FieldPermissionsModal.tsx`**: قسم «منح فردي — تطوير محاكي بناء شبكة FTTH» أسفل الشبكة: `EmployeeSearch` بوضع controlled (`value/onChange/onSelect`, `searchUsername`, `portalClassName="z-[100000]"`) + شرائح ممنوحين بزر إزالة. الجلب عبر embed مُسمّى `profiles!field_user_permissions_user_id_fkey(full_name, job_number)` (تمييزاً عن FK المانح)، وغاية `handleSave` تحفظ **الفروقات فقط**: insert المضافين (بـ `granted_by = auth.uid()`) وdelete المسحوبين (`.in('user_id', …)`) — خطأ أي منها يفشل الحفظ بكله. غياب الجدول (SQL لم يُطبَّق) ⇒ قسم فارغ دون تعطيل النافذة.

### النشر والتحقق
بناء نظيف (3990 وحدة، exit 0) → SEED `cp -a dist dist_stage` → WinSCP `synchronize -criteria=size` (عِدّة chunks تغيّر هاشها تسلسلياً + `EmployeeSearch`/`FiberSimulatorLauncher` + CSS + `index.html`) → SWAP مع إبقاء `dist_old` حتى التحقق → الموقع 200 ويقدّم `index-DcXjY37h.js` (NEW-BUNDLE-LIVE) → حذف `dist_old` والملفات المؤقتة. **ملاحظة**: filemask `assets/; index.html` في أول محاولة أفرغ المزامنة (القناع يطابق أسماء الملفات لا المسارات) — حُذف القناع فاعتمد criteria=size وحده.

### متبقٍ على المستخدم (اختبار)
1. **مستويات**: منح «تطوير محاكي FTTH» لمستوى (مثلاً إعلام) ⇒ يظهر لحسابات الإعلام؛ وأي حقل مالي مُقيَّد بمستوى يصبح للتو قابلاً للقراءة/القفل الصحيح لغير المطورين (تأثير جانبي مقصود لفتح القراءة).
2. **منح فردي**: نفس النافذة → قسم «منح فردي» → بحث باسم بشير (أو أي موظف خارج المستويات) → «حفظ» ⇒ تظهر له البطاقة ولو كان مستواه مسحوباً؛ إزالته وحفظ ⇒ تختفي (خلال ≤60 ث أو إعادة تحميل).

---

## 2026-08-30 — محاكي FTTH يتحول من حصرية المطور إلى «صلاحيات الحقول» (منح/سحب لكل مستوى)

### إصلاح لاحق: المنح لا يصل لغير المطور — RLS صارم على field_permissions
**العَرَض**: مُنح «عام» لـ ftth_simulator من النافذة (حُفظ فعلاً — الصف `{4}` موجود في القاعدة)، لكن المشرف العام (بشير) لا يرى البطاقة.

**التشخيص من القاعدة مباشرة (psql عبر SSH)**: سياسات RLS الفعلية على الجدول **ليست** المسموحة في `create_field_permissions.sql` بل صارمة: `Strict field_permissions read|modify` بشرط `get_my_admin_role() = 'developer'` — أي أن **قراءة الجدول محصورة بالمطور**؛ استعلام أي مستخدم آخر يعود فارغاً بلا خطأ → fail-closed → إخفاء. (وهذا يعني أيضاً أن مستويات isFieldReadOnly للحقول المالية لا تصل لأي غير مطور أصلاً — سلوك قائم لم يُمَس).

**الحل المعزول** (بدل فتح سياسة القراءة — كي لا يتغير سلوك الحقول المالية):
- `sql-files/grant_ftth_read_rpc.sql`: دالة `get_ftth_simulator_permission_levels()` — `SECURITY DEFINER`، `STABLE`، `SET search_path=public`، تُعيد مصفوفة المستويات لسجل `ftth_simulator` **فقط** وللمصادقين فقط (`auth.uid() IS NOT NULL`)، مع `REVOKE FROM PUBLIC/anon` و`GRANT EXECUTE TO authenticated`. نُفِّذت فعلياً على قاعدة الـ VPS.
- `useFtthSimulatorAccess.ts`: النداء صار `supabase.rpc('get_ftth_simulator_permission_levels')` بدل select المباشر المحجوب بـ RLS؛ أي خطأ/نتيجة غير مصفوفة → رفض (fail-closed).
- `FieldPermissionsModal.tsx`: أُصلح الافتراضي المضلل — مسارا الخطأ والدمج كانا يستخدمان `[4]` الثابتة فتظهر ftth_simulator «مفعّلة» شكلياً عند غياب الصف؛ أصبحا يستخدمان `defaultLevelsFor` (مخفية `[]`).
- تحقق: PostgREST يعيد `42501 permission denied` لمفتاح anon (مرفوض كما صُمم)؛ والبناء نظيف ثم نشر تفاضلي.

### المطلوب
بطاقة «محاكي بناء شبكة FTTH» في تبويب «عزز معلوماتك» كانت تظهر لحسابات المطورين فقط. المطلوب: يتحكم المطور بظهورها/إخفائها لأي حساب من نافذة «إدارة الحقول → صلاحيات الحقول» بإضافة قسم «تطوير محاكي بناء شبكة FTTH»، مع افتراضي آمن (مخفية عن الجميع حتى يمنحها).

### التصميم
- **المنح على مستوى الأدوار** (دلالات الجدول القائم): `field_permissions.permission_levels INTEGER[]` — 1=مالية، 2=موارد بشرية، 3=إعلام، 4=عام. تعيين الدور→المستوى مطابق لمنطق `isFieldReadOnly` في `useEmployeeManager`؛ الزائر بلا مستوى (مرفوض دوماً).
- **تجاوز دائم لحساب المطور** (`isDeveloperAccount`) داخل نظام المنح — لا يعتمد على محتوى الجدول.
- **fail-closed**: غياب السجل، خطأ الشبكة، أو مصفوفة فارغة `'{}'` ⇒ إخفاء. `checking` يخفي البطاقة حتى الحسم كي لا تومض للغير ممنوح.

### الملفات
- **جديد** `src/features/fiber-simulator/security/ftthAccess.ts`: منطق نقي — `permissionLevelForUser`، `evaluateFtthAccess` (ترتيب الحسم: تجاوز المطور → مستوى المستخدم → الصف → fail-closed). مفتاح الميزة: `FTTH_SIMULATOR_FIELD_KEY = 'ftth_simulator'`.
- **جديد** `src/features/fiber-simulator/security/useFtthSimulatorAccess.ts`: الخطاف المعزول — كاش جلسة (Map + TTL 60 ث)، المطور مسموح بلا نداء شبكي، نداء واحد `select('permission_levels, permission_level').eq('column_name','ftth_simulator').maybeSingle()`، أي خطأ ⇒ مرفوض، حماية ضد الإلغاء بـ flag.
- **تعديل** `FieldPermissionsModal.tsx`: قسم «ميزات حصرية بالمنح» بحقل «تطوير محاكي بناء شبكة FTTH»، ومساعد `defaultLevelsFor` يعيد `[]` (مخفٍ) لهذا المفتاح بدل `[4]` — مطبق في مسار الخطأ والدمج والـ JSX.
- **تعديل** `FiberSimulatorLauncher.tsx`: استُبدلت بوابة `isDeveloperAccount` بالخطاف الجديد (`checking || !allowed ⇒ null`)، وتعديل شارة البطاقة إلى «قيد التطوير — وفق صلاحية معتمدة». بوابة الشاشة (≥7 بوصة) كما هي.
- **جديد** `sql-files/add_ftth_simulator_permission.sql`: `ADD COLUMN IF NOT EXISTS permission_levels INTEGER[] NOT NULL DEFAULT '{4}'` (احتياط إن غاب عن قاعدة قديمة) + `INSERT ... ('ftth_simulator', '{}') ON CONFLICT DO NOTHING`.

### ملاحظات
- حفظ النافذة من المودال يستخدم upsert فيُنشئ السجل تلقائياً لو غاب — لكن تنفيذ الـ SQL يدوياً يضمن السجل من أول مرة.
- لا تعارض مع الميزات الأخرى: المفتاح جديد، الكود كله معزول داخل `security/` الميزة، ولا لمس لمنطق `isFieldReadOnly` الحالي.

### النشر
بناء نظيف (3990 وحدة)، نشر تفاضلي (بَذْر `dist_stage` بـ `cp -a`، `synchronize -criteria=size` رفع الفروقات فقط: `FiberSimulatorLauncher-BJ6u8dqT.js`، `index-Cd-b7sKK.js` وغيرها + `index.html` إجبارياً)، `SWAP_OK`، والموقع يقدّم `index-Cd-b7sKK.js` وحزم المحاكي تعيد 200.

### متبقٍ على المستخدم
1. تنفيذ `sql-files/add_ftth_simulator_permission.sql` في Supabase Studio على الـ VPS (إن لم يُنفَّذ).
2. اختبار من حساب المطور: إدارة الحقول → صلاحيات الحقول → منح «تطوير محاكي بناء شبكة FTTH» لمستوى (مثلاً عام=4) ⇒ البطاقة تظهر لحسابات ذلك المستوى؛ سحبها ⇒ تختفي (مع كاش 60 ث/إعادة تحميل).

---

## 2026-08-29 (5) — إزالة R2 نهائياً + حصرية صورة الدخول بالمقعد الأول

### الإزالة النهائية للمسار الخارجي (قرار المستخدم)
بيئة الوزارة مقفلة خارجياً: أي اتصال خارجي يتطلب «ممراً أمنياً» (اعتماد جدار حماية) غير متاح — حتى `onesignal.com` ما تزال معطلة بانتظاره. نداء الاحتياط Vercel→R2 كان يصدر من **متصفح الموظف** لا من الخادم (الخادم محجوب عن vercel.app — مهلة exit 28)، لكن الاستنتاج واحد: حتى لو نجح الرفع الاحتياطي فروابط `r2.dev` لن تظهر في متصفحات مقفلة أصلاً. فالاحتياط عديم الفائدة عملياً.

- حُذف `r2Storage.ts` بالكامل، وأُنشئ `snapshotStorage.ts`: رفع محلي خالص إلى حاوية `attendance-snapshots` (same-origin عبر `khr-itpc.egov.iq/storage/v1`) بالعميل المخصص بمفتاح التطبيق، 3 محاولات تلقائية، بلا أي مسار خارجي.
- حُدِّث الاستيراد والنداء في `AttendanceCheckInOut.tsx` (السطران 9 و228) وتعليق `supabase.ts:27`.

### إصلاح حصرية المقاعد في تقارير المشرف (`Timesheets.tsx`)
المشكلة المُبلَّغة: بعد بصمة دخول + بصمة خروج ظهرت **صورتان في المقعد الثاني** (صورة الدخول مكررة + صورة الخروج). المطلوب: صورة البصمة الأولى مقتصرة بالمقعد الأول دون نقل أو تكرار.

الحل: في خلية المقاعدين، تُرشَّح صورة الدخول (`check_in_snapshot_url`) من لقطات اليوم قبل عرضها في مقعد الخروج:
- `restSnaps = daySnaps.filter(s => s.url !== checkInUrl)` — وإن غابت صورة الدخول (فشل رفع سابق) تبقى كل اللقطات للخروج (توافق مع السجلات القديمة).
- مقعد الخروج يعرض الأحدث دائماً (`restSnaps[last]`، واحتياطاً `check_out_snapshot_url` للسجلات القديمة بلا raw_punches)، والبقية في المعرض المطوي «لقطات ما بعد الدخول (N)».

سلوك تراكم بصمات الاستراحة في المقعد الثاني **مقصود ومتوافق**: الأحدث ظاهرة والبقية مطوية في المعرض، بلا صورة الدخول.

### النشر
بناء نظيف (`AttendanceCheckInOut-UhjggqMn.js` — محلياً: `attendance-snapshots: 2` / `upload-snapshot(vercel): 0` / `r2.dev: 0`)، نشر محروس `SWAP_OK`، الخادم يقدّم `index-DSO4UDsu.js`، وفحص الخادم: `vercel = 0`. (انقطع الرفع أول مرة بإغلاق المحرر خطأً ثم أُعيد بنجاح — النمط المحروس ضمن ألا يتأثر الموقع الحي أثناءه).

### تحسين لاحق بالنمط التفاضلي (اقتراح المستخدم)
استُبدل `put *` الكامل (يعيد رفع ~200+ MB من النماذج/الخطوط/الأصوات الثابتة) بنمط تفاضلي: يُزرَع `dist_stage` بنسخة `dist_prev` على قرص الخادم (`cp -a` — بلا شبكة)، ثم `synchronize remote -criteria=size` يرفع الفروقات فقط (الحزم المُلبَّدة بأسماء متغيرة)، مع رفع `index.html` إجبارياً دائماً (تطابق الحجم قد يخفي تغيير محتواه). اختُبر فعلياً: نُقل ~2 MB فقط (55 حزمة) بدل الرفع الكامل، و`SWAP_OK` سليم. التبديل المحروس لم يتغير.

### إزالة تحذير بناء CSS (`-: .;` — esbuild css minify)
تحذير «Expected identifier but found "-"» كان مصدره رمز صفيف محارف بصيغة صنف اعتباطي داخل `snapshotStorage.ts` (تنظيف الطابع الزمني): ماسح Tailwind يقرأ النص الخام ويولّد قاعدة `.\[-\:\.\]{-: .}` التالفة. حُوِّل التنظيف إلى `/[^0-9]/g` (أرقام فقط)، مع صياغة التعليق بلا أقواس مربعة كي لا يعود الرمز من داخل مثال نصي (حدث فعلاً في محاولة أولى!). البناء الآن نظيف بلا تحذير، والقاعدة التالفة اختفت من `index.css` المبني. نُشر بالنمط التفاضلي (`SWAP_OK`، `index-B3tX6jK9.js`).

---

## 2026-08-29 (4) — الحقيقة الكاملة: مسار الصور الأصلي كان خارجياً (Vercel→R2) وقد انقطع

### الاكتشاف
الكود الأصلي لرفع صور البصمات (`r2Storage.ts`) كان — قبل هذه السلسلة — ينادي دالة سيرفرلس على `https://itpc-hr.vercel.app/api/upload-snapshot` ترفع إلى Cloudflare R2 وتعيد روابط `pub-...r2.dev`. أي أن **رفع الصور وحده بقي خارجياً** بعد ترحيل التطبيق والقاعدة إلى خادم الوزارة (بقية النظام محلي 100% عبر `khr-itpc.egov.iq`).

### الدليل من قاعدة البيانات نفسها
البصمات الناجحة (17/8، 18/8، و27/8 الساعة 19:03 UTC) روابط صورها كلها على `r2.dev` — النسخة المنشورة كانت تستخدم المسار الخارجي فعلاً.

### سبب الانقطاع ليلة 27/8
خدمة Vercel الخارجية كانت تعيد 500 من ~19:38 حتى ~00:11 UTC ثم تعافت. أثناءها فشل رفع كل صورة («Internal Server Error» في الملاحظات) بينما البصمة نفسها تثبت. لا خلل في كاميرا/تعرف/قاعدة.

### الاختبارات الأربعة الحاسمة
1. Vercel من جهاز التطوير: POST **200** وأعاد رابط R2 صالحاً.
2. Vercel من الخادم نفسه: **مهلة كاملة** (الخادم محجوب عن vercel.app — لا يصلح احتياطاً سيرفر@).
3. تخزين الوزارة عبر `10.56.3.3/storage/v1` بمفتاح التطبيق: **200**.
4. تخزين الوزارة عبر النطاق الرسمي `https://khr-itpc.egov.iq/storage/v1`: رفع **200** والقراءة العامة **200** — الوكيل الأمامي يمرر `/storage/v1`، فروابط الصور ستظهر في تقارير المشرف من النطاق الرسمي.

### الإصلاح النهائي v4 (`r2Storage.ts`)
- **المسار الأساسي**: رفع مباشر إلى حاوية `attendance-snapshots` على تخزين خادم الوزارة (same-origin) عبر العميل المخصص بمفتاح التطبيق، مع 3 محاولات تلقائية وكسر فوري لأخطاء الصلاحيات.
- **الاحتياط التلقائي**: المسار القديم Vercel→R2 بمهلة 15 ثانية — إن تعطل أحد المسارين مستقبلاً يستمر الرفع عبر الآخر.
- تصحيح خطأ بناء عابر أثناء التطوير: قوس `]` بدل `)` في حلقة `atob` (السطر 98).

### النشر
بناء ناجح (`AttendanceCheckInOut-Dkk1Eaw7.js`، تأكد احتواؤه المسارين)، نشر محروس `SWAP_OK`، الخادم يقدّم `index-CmyqXXN6.js`، وكود v4 مؤكد على الخادم.

### التحقق المتبقي
بصمة اختبار فعلية يوم العمل التالي (الأحد 30/8) والتأكد من ظهور الصورة في تقرير المشرف برابط `khr-itpc.egov.iq/storage/...`.

### مسألة جانبية مكتشفة (لم تُعالج)
`POST /functions/v1/send-notification` (الإشعارات/ntfy) يعيد 500 مراراً منذ 27/8 — تحتاج معالجة منفصلة.

---

## 2026-08-28 (3) — السبب الجذري النهائي: تخزين الصور يرفض رمز جلسة المستخدم (500)

### الدليل القاطع (بعد نشر الإصلاح الأول)
أول بصمة اختبار على النسخة المحصّنة كتبت السبب بنفسها في ملاحظاتها:
`(فشل رفع الصورة للسحابة: Internal Server Error)`.
وسجلات حاوية `supabase-storage` تُظهر طلب curl الاختباري (بمفتاح التطبيق anon ← **200**) **ولا يوجد أي أثر لطلب المتصفح** — أي أن طلب المتصفح يُرفض قبل معالجته أصلاً.

### التفسير
مكتبة supabase-js تستبدل ترويسة `Authorization` في طلبات التخزين بـ**رمز جلسة الموظف المسجَّل** (JWT المستخدم) بدل مفتاح التطبيق، وخدمة التخزين على هذا الخادم ترفض رموز المستخدمين بخطأ 500 (متسق مع خطأ `ERR_JWS_SIGNATURE_VERIFICATION_FAILED` الظاهر سابقاً في سجلاتها، ويفسر فشل **كل** رفعات المتصفح منذ بداية المشكلة بينما تعمل استعلامات القاعدة لأنها تمر بخدمة PostgREST التي تقبل الرمز). لذلك: curl بمفتاح التطبيق ينجح، والمتصفح بجلسة الموظف يفشل — وهذا هو التناقض الذي أبهم التشخيص طويلاً.

### الإصلاح
1. **`r2Storage.ts`**: عميل تخزين مخصص (`createClient` بجلسة معطّلة كلياً: `persistSession/autoRefreshToken/detectSessionInUrl = false`) — تبقى ترويسته مثبتة على مفتاح التطبيق دائماً كما في الطلب الناجح. **3 محاولات تلقائية** قبل تثبيت البصمة مع كسر فوري لأخطاء الصلاحيات.
2. **`supabase.ts`**: تصدير `supabaseUrl` و`supabaseAnonKey` لاستخدام المفتاح نفسه في العميل المخصص.
3. **`AttendanceCheckInOut.tsx`**: حذف نصيحة «إعادة المحاولة» من التنبيه (ملاحظة المستخدم: إعادة المحاولة تحسب بصمة جديدة) — النص الجديد: «تم تسجيل البصمة، لكن تعذر حفظ الصورة — يرجى إبلاغ المسؤول».

### ملاحظة مستقبلية (خيار خادمي)
الحل أعلاه من جهة التطبيق. الحل الخادمي البديل عند توفر صيانة: مواءمة إصدارات حاويات Supabase أو ضبط مفاتيح توقيع Auth/Storage حتى تقبل التخزين رموز المستخدمين — يبقى الإصلاح الحالي صحيحاً وآمناً في الحالتين لأن الحاوية تسمح بالرفع بمفتاح التطبيق.

### التحقق والنشر
`tsc` نظيف + بناء ناجح. نشر بتبديل محمي: `SWAP_OK`، فحوصات `SPA_3001=200` / `ORIGIN_80=200` / الحزم الجديدة `200`، والصفحة الحية تقدّم `index-BOUVxwyb.js`.

---

## 2026-08-28 (2) — إصلاح فشل رفع صور البصمات إلى التخزين (المقاعد فارغة في التقارير)

### الأعراض
- البصمات تُسجَّل بنجاح لكن ملاحظتها في قاعدة البيانات: «(فشل رفع الصورة للسحابة)» — كل البصمات منذ الخميس 19:38 UTC.
- في تقارير المشرف يظهر المقعد الأول والثاني فارغين لأن `snapshot_url = null` أصلاً في `raw_punches` (فلا صورة ليُعرضها التقرير — العرض والحفظ والمصنّف كانت سليمة جميعاً).

### التشخيص (بالأدلة من الخادم)
1. سجلات حاوية `supabase-storage`: لا يوجد أي POST من المتصفح بعد 20:46 UTC — أي أن الطلب يفشل **قبل الخروج من المتصفح**.
2. **الاختبار الحاسم**: استخراج مفتاح anon من الحزمة المنشورة ورفع اختباري عبر Kong (`POST /storage/v1/object/attendance-snapshots/...`) من الخادم ← **200**، وفحص GET العام ← **200**. فالمسار (المفتاح + Kong + الحاوية) سليم تماماً من الخادم.
3. إذن الخلل متصفحي فقط، والملاحظة «فشل رفع» تعني أن `captureFrame` أعاد قيمة نصية صحيحة لكنها **ليست صورة**: عند الالتقاط قبل جاهزية بث الكاميرا تكون `videoWidth = 0` فتُرسم لوحة 0×0 ويعيد `toDataURL` السلسلة `"data:,"` (نص صحيح truthy!)، ثم في `base64ToBlob` ينفجر `atob('data:,')` باستثناء يُبتلع في catch صامت ويعيد null ← الملاحظة نفسها في كل بصمة.

### الإصلاح (ثلاث طبقات)
1. **`useCamera.ts` — `captureFrame`**: انتظار جاهزية الفيديو (`readyState ≥ 2 && videoWidth > 0` حتى 3 ثوانٍ برسالة واضحة)، رفض الأبعاد < 16px، والتحقق من صحة صيغة الـdata URL قبل إعادتها («data:,» تُرفض صراحة).
2. **`r2Storage.ts` — `uploadSnapshotToR2`**: تحليل صارم لصيغة الصورة ورفض الفارغة، **إعادة محاولة واحدة** عند فشل عابر، وإرجاع `{ url, error }` مع **سبب الفشل المفصّل** بدل null الصامت.
3. **`AttendanceCheckInOut.tsx`**: ملاحظة البصمة تتضمن سبب الفشل بالتفصيل «(فشل رفع الصورة للسحابة: <السبب>)» للتشخيص عبر القاعدة، و**تحذير فوري أصفر للموظف** عند فشل الصورة بدل نجاح صامت.

### التحقق والنشر
- `tsc --noEmit` نظيف + بناء ناجح (`index-Djry0FBk.js`).
- نشر بتناوب آمن مع حماية (لا تبديل إلا إذا وُجد `index.html` في المجلد المرحلي): `SWAP_OK`، فحوصات: `SPA_3001=200`، `ORIGIN_80=200`، الحزمة الجديدة والجسر `AttendanceCheckInOut` كلاهما `200`.

---

## 2026-08-28 — إصلاح مشكلتين ظهرتا في تجربة النسخة المنشورة

### المشكلة 1: مقعد الصورة الثاني (الخروج) يظل فارغاً رغم عمليات البصمة

**الأعراض:**
- الأيام القديمة (قبل التطوير) كان مقعد الخروج يعرض الصور المطوية ويتيح الاختيار بشكل سليم.
- البصمات الحديثة لا تظهر في المقعد الثاني إطلاقاً — يظل فارغاً رغم تسجيل انصراف أو دخول ثانٍ.

**السبب الجذري:**
مقعد الخروج في `Timesheets.tsx` كان يعتمد حصراً على حقل `check_out_snapshot_url` لعرض الصورة ولفتح القائمة المطوية. هذا الحقل لا يُملأ في قاعدة البيانات إلا حين تصنّف المصنّفُ (`punchCategorizer.ts`) آخرَ بصمة في خانة «الانصراف النهائي»، أي فقط عند اكتمال **2 أو 4 أو 6 بصمات**:

- بصمتان (دخول + انصراف) ← يُملأ `check_out_snapshot_url` ✓
- ثلاث بصمات (دخول + خروج زمني + عودة) أو خمس ← الحقل يبقى `null` ✗

الأيام القديمة نجحت مصادفةً لأن بصماتها اكتملت (2/4/6)، أما البصمات الحديثة غير المكتملة فأبقت الحقل فارغاً فبقي المقعد الثاني فارغاً — رغم أن الصور **محفوظة سليمة** داخل `raw_punches[].snapshot_url` (آلية الحفظ في `registerPunch` سليمة ولم تكن يوماً جزءاً من المشكلة).

كانت هناك مشكلة ثانوية كذلك: شرط فتح المعرض المطوي كان `daySnaps.length > 2` أي لا ينفتح إلا فوق لقطتين، بينما المطلوب أن ينفتح عند أي تعدد لقطات (≥ 2).

**الإصلاح** (`src/features/attendance/components/Timesheets.tsx`):
- المقعد الثاني يعرض الآن **آخر لقطة دائماً** (`lastSnapUrl`): مشتقة من آخر عنصر في لقطات `raw_punches` المستخرجة عبر `extractPunchSnapshots`، مع احتياط إلى `check_out_snapshot_url` للسجلات القديمة الخالية من `raw_punches`.
- **القائمة المطوية تنفتح دائماً عند توفر أكثر من لقطة** (الشرط أصبح `> 1` بدل `> 2`) — سواء للنقر على المقعد أو لظهور شارة العدد.
- حالة لقطة واحدة فقط (دخول فقط): يبقى المقعد الثاني فارغاً كي لا يكرر صورة الدخول نفسها.
- لم يُمسّ أي منطق حفظ أو حذف — لا تُحذف أي صورة، والباقي مطوي في المعرض.

**النتيجة على كل أنواع البصمات:**
| عدد البصمات | قبل | بعد |
|---|---|---|
| 1 (دخول فقط) | المقعد فارغ | المقعد فارغ (لا تكرار لصورة الدخول) |
| 2 (دخول + انصراف) | صورة + تكبير مباشر | صورة + معرض مطوي (لقطتان للاختيار) |
| 3 (دخول + خروج زمني + عودة) | **المقعد فارغ — الخلل** | صورة العودة + معرض مطوي (3 لقطات) |
| 4 (زوجان مكتملان) | صورة + معرض | صورة + معرض مطوي (4 لقطات) |
| 5 | **المقعد فارغ — الخلل** | صورة + معرض مطوي (5 لقطات) |

---

### المشكلة 2: بطاقة تثبيت الحضور والانصراف لا تنتقل لليوم الجديد عند تحوّل التاريخ

**الأعراض:**
موظف سجّل بصمات يوم الخميس 27/8/2026، وبعد منتصف الليل (انتقال التاريخ إلى 28/8/2026) تظل بطاقة التثبيت تعرض بصمات الخميس القديمة بدلاً من بطاقة يوم جديد فارغة. المنطق «كان يعمل سابقاً وبشكل جيد» ثم تعطّل.

**السبب الجذري — خطأ توقيت UTC (السبب الرئيسي):**
كل استعلامات «يوم البصمة» كانت تعتمد:

```ts
new Date().toISOString().split('T')[0]   // تاريخ UTC وليس المحلي!
```

`toISOString()` يرجع توقيت UTC، وبغداد **UTC+3** (بلا توقيت صيفي). النتيجة: بين **منتصف الليل المحلي و 03:00 فجراً** يظل «تاريخ اليوم» بحساب UTC هو **الأمس**. فالاستعلامات تُرجع سجل الخميس بعد منتصف الليل مباشرة — وهو وقت الاختبار بالضبط (بناء الجلسة السابقة كان 22:17 وبدأ الاختبار بعد منتصف الليل).

السبب الثاني — غياب مراقب التحوّل: `AttendanceDashboard` كان يجلب `todayAttendance` مرة واحدة عند التركيب فقط؛ فحتى لو صحّح الاستعلام، من يُبقي الصفحة مفتوحة عبر منتصف الليل لا يُعاد له الجلب تلقائياً. (منطق «اليوم الجديد الفارغ» نفسه كان يعمل — الخلل كان فيما يُغذّيه من تاريخ ومن عدم إعادة الجلب.)

كما أن عمود `created_at` نوعه `timestamptz` في قاعدة البيانات، فكان الاستعلام بحدود `${date}T00:00:00` يُفسَّر UTC في الخادم أيضاً، فيوسّع نافذة الخطأ إلى 3 ساعات كاملة.

**الإصلاح:**

1. **`src/features/attendance/services/attendanceService.ts`** — أداتان جديدتان:
   - `getLocalDateStr()`: تاريخ اليوم المحلي `YYYY-MM-DD` وفق توقيت جهاز المستخدم (مُصدَّرة لاستخدام الواجهات).
   - `localDayRangeUTC(dateStr)`: تحوّل حدود اليوم المحلي (00:00 و 23:59:59 محلياً) إلى UTC ISO للاستعلام الصحيح من عمود `timestamptz`.
   - استُبدلت التواريخ في: `getTodayByEmployeeId` و `registerPunch` (استعلام اليوم + استعلام الأمس لمنطق الخفرة الليلية) و `getByDate`.

2. **`src/features/attendance/components/AttendanceDashboard.tsx`** — مراقب تحوّل التاريخ:
   - حالة `viewedDate` تحفظ تاريخ اليوم لحظة العرض.
   - فحص كل 30 ثانية + عند عودة التبويب للواجهة (`visibilitychange` و `focus` — لأن المتصفح يخنق المؤقتات في الخلفية).
   - فور اختلاف `getLocalDateStr()` عن المخزون ← يُعاد جلب سجل اليوم ← **بطاقة يوم جديد فارغة فوراً دون أي تحديث يدوي للصفحة**.
   - سيناريوهات التحوّل المغطاة: منتصف الليل والصفحة مفتوحة بالواجهة (≤30 ث)، الصفحة خلفية/مخفية ثم تُستعاد (فحص فوري عند الظهور)، الجهاز نائم ثم يفيق، وتغيير التاريخ اليدوي في النظام.
   - إصلاح ملازم: حدود شهر الإحصائيات (firstDay/lastDay) كانت تعاني نفس خطأ UTC — أصبحت عبر `getLocalDateStr`.
   - أُزيل `exceptions` من تفكيك `useAttendance` (خاصية غير موجودة أصلاً — بقايا قديمة كانت ستُفشل الفحص النوعي).

3. **`src/features/attendance/components/AttendanceCheckInOut.tsx`**:
   - `todayStr` لبانرات الإجازات أصبح `getLocalDateStr()` بدل UTC — وإلا كانت إجازات اليوم السابق تظهر خطأً بين منتصف الليل و03:00.

---

### ملاحظات جودة
- جميع التحديثات فورية دون تحديث يدوي: بطاقة اليوم، مقعد الصورة الثاني، بانرات الإجازات.
- لم يتغير أي مخطط قاعدة بيانات ولا أي مسار حفظ — الإصلاحات عرضية/استعلامية فقط، فلا أثر رجعي على البيانات القديمة (تُعرض كما هي عبر مسار الاحتياط).
- الفحص النوعي `tsc --noEmit` وبناء الإنتاج نظيفان، ثم نُشر البناء على خادم الوزارة.
